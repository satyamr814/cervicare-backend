version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - HOST=0.0.0.0
      - PORT=8000
      - RELOAD=false
    volumes:
      # Mount backend directory to ensure model is accessible
      - ./backend:/app/backend
    restart: unless-stopped

